const nombre = 'monitor 20 pulgadas';
const precio = 300;
const discponible = true;
// un objeto agrupa todo en un a sola variable
// Object literal
const producto = {
    nombre: "monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}
console.log(producto);
